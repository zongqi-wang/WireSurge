# WireSurge Desktop

This directory is reserved for the Tauri desktop shell. The first desktop milestone should wrap the shared web UI, expose native filesystem/keychain commands, and read the same local runner registry that the CLI writes under `.wiresurge/runners`.

The initial Runners UI contract is visible in `apps/web/index.html`.

